environments:
  dev:
    project: etl-teste-tecnico-503722
    region: us-east1
    composer_environment: composer-teste-tec-etl
    artifact_storage:
      bucket: teste_tec_etl_airflow
      path_prefix: "empresa-etl-"
    pipelines:
      - source: 'orchestration_pipeline.yaml'
